        for fromNode, toNode in enumerate(edges):
            score[toNode] += fromNode

        maxIndex = 0
        for idx, val in enumerate(score):
            if val > score[maxIndex]:
        score = [0] * len(edges)
    def edgeScore(self, edges: List[int]) -> int:
class Solution:
                maxIndex = idx
        return maxIndex
        return score.index(max(score))

[
